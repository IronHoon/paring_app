import React from 'react';
import FullWidthSlide from '../../../atoms/carousel/FullWidthSlide';

const BannerSection = ({ banners }) => {
  return <FullWidthSlide data={banners} />;
};

export default BannerSection;
